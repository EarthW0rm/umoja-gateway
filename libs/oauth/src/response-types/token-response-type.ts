import { ServerException } from '../exceptions';

/**
 * Placeholder response type for implicit token flows.
 * @throws {ServerException} Always, until implemented.
 */
export class TokenResponseType {
  /**
   * Creates the token response type placeholder.
   */
  constructor() {
    throw new ServerException('Not implemented.');
  }
}
